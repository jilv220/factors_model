[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "factors-model"
version = "0.1.0"
description = "Versioned command-line baselines for bad-beta and six-factor equity-ranking pipelines."
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
  "numpy==2.5.2",
  "pandas==3.0.5",
  "xlrd==2.0.2",
  "yfinance==1.5.2",
]

[project.scripts]
factors = "factors_model.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
