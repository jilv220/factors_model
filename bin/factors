#!/bin/sh
set -eu

repo_root=$(CDPATH= cd -- "$(dirname -- "$0")/.." && pwd)
project_python="$repo_root/.venv/bin/python"

if [ ! -x "$project_python" ]; then
  echo "factors: local runtime missing; run ./bin/setup first" >&2
  exit 2
fi

exec "$project_python" -m factors_model "$@"
